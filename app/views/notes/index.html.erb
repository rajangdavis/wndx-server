<br>
<% @notes.each do |note|%>
	<span><%= link_to "#{note.description}", note_path(note) %></span><br>

	<%=link_to "#{note.username}", user_path(note.user_id)%> | <%= note.created_at.strftime("%m/%d/%Y")%> | 
	<%=link_to note.comments.count, note_path(note) %><br><br>

<% end %>

<% if current_user %>


    <%= text_field_tag :description %>
    <%= hidden_field_tag :domain %>
    <%= hidden_field_tag :user_id, current_user.id %>
 
<div class="actions"><span id="wndx-addnote">Add note</span></div>

<% end %>

<script type="text/javascript">
$(document).ready(function(){
  $(document).on('click','#wndx-addnote', function() {
      
            console.log('hello?');
  var AUTH_TOKEN = $('meta[name="csrf-token"]').attr('content');
  $.ajaxSetup({
      beforeSend: function(xhr) {
      xhr.setRequestHeader('X-CSRF-Token', $('meta[name="csrf-token"]').attr('content'));
      }
    }); 

  $.ajax({
      type: "POST", 
      url: "/notes?&authenticity_token="+encodeURIComponent(AUTH_TOKEN), 
      data:{ utf:"✓", description : $('#description').val() , domain : $('#domain').val(), user_id : $('#user_id').val(),commit: "submit" },
      dataType: "HTML",
      success:function(data) {
            console.log(data);
            // window.location = data.redirect_url;
        },
        error: function(xhr, status) {
            console.log(status);
        }
      });

  return false;
  }); 
});
</script>